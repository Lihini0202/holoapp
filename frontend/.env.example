# Flutter configuration. Copy to .env and fill in; .env is never committed.
# This file is bundled as an asset, so it must contain only public keys.
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key
BACKEND_URL=https://your-backend-host
