# Choreo component descriptor. The version is quoted: unquoted, YAML would read

schemaVersion: "1.2"
endpoints:
    - name: holo-api
      displayName: Holo Analysis API
      service:
        basePath: /
        port: 8000
      type: REST
      networkVisibilities:
        - Public
